# Do Not Mark Production Yet

This repository should not be marked fully production-ready until:

1. CI passes.
2. Mock proof endpoints are removed or converted to explicit `not_configured` errors.
3. Admin API routes are authenticated.
4. A signed sample receipt is generated and documented.
5. A release is cut with checksums.

This branch is a hardening baseline, not the final production release.
